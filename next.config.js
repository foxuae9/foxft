/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['fott.moxblxaxdw-jqp3v881x650.p.temp-site.link'],
  },
  output: 'standalone',
  distDir: 'dist',
}

module.exports = nextConfig
